## NYC Explorer — Environment Template (Sprint 05 mocks)

# Web server port consumed by Program.cs (defaults to 5000 when unset)
APP_PORT=5000

# Geocoding adapter selection (defaults to mock). If the real provider times out or returns 429, flip back to mock and log it.
GEO_PROVIDER=mock

# Geocoder request timeout (ms). Default 3500 if unset.
GEO_TIMEOUT_MS=3500

# Routing adapter selection (defaults to mock). Real-provider retries are limited to 1; after timeout/429, revert to mock and note the switch.
# Mock route engine auto-enables unless __nycMock.route=false is set in tests
ROUTE_PROVIDER=mock

# Route adapter cooldown (ms) before retrying after rate limits (docs default 60000).
ROUTE_RATE_LIMIT_COOLDOWN_MS=60000
# Geocoder cooldown (ms) before retrying after rate limits (docs default 30000).
GEO_RATE_LIMIT_COOLDOWN_MS=30000

# Reserved for future real providers; leave blank for mocks
# GEO_API_KEY=
# ROUTE_API_URL=
