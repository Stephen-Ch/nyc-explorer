## NYC Explorer — Environment Template (Sprint 05 mocks)

# Web server port consumed by Program.cs (defaults to 5000 when unset)
APP_PORT=5000

# Geocoding adapter selection; only the mock provider is available in Sprint 05
GEO_PROVIDER=mock

# Routing adapter selection; only the mock provider is available in Sprint 05
ROUTE_PROVIDER=mock

# Reserved for future real providers; leave blank for mocks
# GEO_API_KEY=
# ROUTE_API_URL=
